# Parallel AI MCP Server Requirements

# Core MCP Framework
mcp @ git+https://github.com/modelcontextprotocol/python-sdk.git

# Parallel AI SDK
parallel-web>=0.2.0

# Environment management
python-dotenv

# Type hints and validation
pydantic>=2.0.0

# HTTP client
requests

# Async/WebSocket support (for future WebSocket transport)
starlette>=0.27.0
uvicorn>=0.23.0
